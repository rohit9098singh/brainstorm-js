// what is the differnce between client side js and server side js
// ans=>JavaScript that runs inside the user’s browser.
//         Execution Environment

//         Chrome (V8 engine)
//         Firefox (SpiderMonkey)
//         Safari (JavaScriptCore)

//         Used to handle:

//         UI interactions
//         DOM manipulation
//         Form validation

// JavaScript that runs on the server before response is sent to client.
//        Execution Environment

//         Node.js
//         Deno
//         Bun

//         Used to handle:

//         Database operations
//         Authentication
//         Business logic

// What Are Streams?
// A stream is a way to handle data piece by piece (chunk by chunk) instead of loading the entire data into memory at once.

console.log("shivam"-100)